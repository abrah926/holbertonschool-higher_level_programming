#!/usr/bin/python3


def multiple_returns(sentence):
    len_new_tuple = len(sentence)
    if len_new_sentence == 0:
        first_char = None
    else:
        first_char = sentence[0]

    return (len_new_tuple, first_char)